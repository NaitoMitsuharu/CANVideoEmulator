# CAN出力の実機確認

CANVideoEmulatorの担当範囲は、動画に合わせて記録済みCANをPCAN-USBから出力するところまでです。受信機の種類や、その後の転送・表示方式は問いません。

```text
Windows / CANVideoEmulator → PCAN-USB → CANネットワーク → 独立した受信機
```

## 準備

- PCANドライバーとPCAN-Basic APIをインストールする。
- シナリオ、記録CANバス、PCANチャンネルを選択する。
- BITRATEを接続先と一致させる。既定値は500 kbit/sで、元の記録速度を自動検出した値ではない。
- CAN-H、CAN-L、GND、両端の120 Ω終端、受信機の電源を確認する。端子配置は使用機器のマニュアルで確認する。
- 少なくとも1台の受信機を通常動作にする。Listen-onlyの受信機だけではACKが返らない。
- 受信数を比較する場合は、受信フィルター、他ノードの送信、計測の開始・終了時刻を揃える。

`USB Ready`はUSB検出、`Connected`は初期化と状態照会の結果です。無通信時にCAN配線の先の存在や到達を保証するものではありません。

## PLAYで送信を確認する

独立した時間制限テストは削除しました。通常のPLAYで動画とCANを再生し、PAUSEまたはStopで止めます。

1. シナリオと記録バスを選び、受信機のカウンターを記録する。
2. `Transmit to PCAN-USB`を有効にしてPLAYを押す。
3. 映像右上のCANレート、API受付数、送信エラー、Windows側の損失、ジッターと、上部のPCAN状態を確認する。
4. PAUSE/Stop後、キューの排出を待って受信数の差分を記録する。

API受付数はドライバーへ渡した数であり、線上の送信完了数ではありません。ジッターは予定時刻に対するAPI呼び出し時刻のずれです。受信数・ドロップ数は必要に応じて独立した受信機で測定します。デモモードでは物理CANは送信しません。

## 異常と復帰

| 操作・状態 | 確認すること |
|---|---|
| USB未接続で起動 | デモ再生でき、CAN出力がオフである |
| USBを接続 | 約2秒周期で一覧更新。自動送信しない |
| 送信中にUSBを抜く | 検出後に一時停止・チャンネル解放・送信チェック解除 |
| USBを再接続 | 一覧更新後、明示的に送信チェックとPLAYを操作して再開 |
| CAN側を切断／受信機の電源を切る | USB一覧は変わらない。送信時のバス状態、エラー、受信数を観察 |
| BusLight / BusHeavy | 配線、電源、終端、速度、ACKを確認 |
| BUS OFF | 再生が一時停止する。原因解消後、PLAYで再初期化（同じチャンネルを使用する他アプリは終了） |
| Pause / Stop | 新しい送信要求が止まる。既に受け付けられたキューは遅れて排出され得る |

相手がいないと必ずBUS OFFになるわけではなく、Error Passiveで留まる場合もあります。無通信時の正常表示だけで復旧を判断せず、短い送信試験と受信機で確認してください。

## 記録テンプレート

```text
日時 / PC / アプリ版:
PCAN機種 / ドライバー / チャンネル:
シナリオ / 記録バス / BITRATE:
受信機 / フィルター / 終端:
試験時間:
スケジュール数 / API受付数 / エラー:
観測したバス状態 / ジッター:
受信数 / ドロップ数（任意）:
抜去・復帰結果:
動画とCANの同期測定方法 / 誤差:
```

動画の表示と物理CAN送信の同期精度は、API時刻だけでは確定できません。要求精度を決めたうえで、表示フレームと受信CANのタイムスタンプを同時測定してください。
