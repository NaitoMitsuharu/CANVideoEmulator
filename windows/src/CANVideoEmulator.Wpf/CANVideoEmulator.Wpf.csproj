<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <UseWPF>true</UseWPF>
    <RootNamespace>CANVideoEmulator.Wpf</RootNamespace>
    <AssemblyName>CANVideoEmulator</AssemblyName>
    <ApplicationIcon>Assets\app.ico</ApplicationIcon>
    <AssemblyTitle>CANVideoEmulator</AssemblyTitle>
    <!-- win-x64 only: the PEAK driver and PCAN-Basic are Windows-only, and the
         exhibition target is a 64-bit Windows 10/11 machine (requirement 14). -->
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <PlatformTarget>x64</PlatformTarget>
    <SelfContained>false</SelfContained>
    <SatelliteResourceLanguages>en</SatelliteResourceLanguages>
  </PropertyGroup>
  <ItemGroup>
    <!-- Also embedded as a resource so the window can use it at runtime; the
         ApplicationIcon above only stamps the EXE. -->
    <Resource Include="Assets\app.ico" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\CANVideoEmulator.Core\CANVideoEmulator.Core.csproj" />
    <ProjectReference Include="..\CANVideoEmulator.Scenarios\CANVideoEmulator.Scenarios.csproj" />
    <ProjectReference Include="..\CANVideoEmulator.Pcan\CANVideoEmulator.Pcan.csproj" />
    <ProjectReference Include="..\CANVideoEmulator.Video\CANVideoEmulator.Video.csproj" />
  </ItemGroup>
</Project>
